name: hello-matrix
on:
  push:
    branches:
      - main

jobs:
  printer:
    runs-on: ubuntu-latest
    uses: ./reusable.yaml
    with:
      my_name: Abra kadabra
    secrets:
      foreign_name: ${{ secrets.foreign_name }}
